/*
Все динамические структуры данных реализовывать через классы. Не использовать STL.
Для каждой динамической структуры должен быть предусмотрен
стандартный набор методов - добавления/удаления/вывода элементов. Во всех задачах обязательно
наличие дружественного интерфейса. Ввод данных с клавиатуры.

 Дан односвязный линейный список и указатель на голову списка P1. Необходимо
вставить значение M перед каждым вторым элементом списка, и вывести ссылку на последний
элемент полученного списка P2. При нечетном числе элементов исходного списка в конец
списка вставлять не надо.
*/
#include <iostream>
//FIX ME: Добаввила необходимые библиотеки
#include <Windows.h>
#include <limits>
#include <string>
#include <sstream>
#include "Header.h"
//FIX ME: Перенесла все скобки на новую строку
//FIX ME: Убрала глобальное использование using namespace std;
//FIX ME: Разделила на модули
//FIX ME: Переименовала функции. Теперь они начинаются с заглавной буквы

int main()
{
    //FIX ME: Добавила наличие русского языка
    setlocale(LC_ALL, "rus");
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);

    LinkedList list;                     // Создаем список
    //FIX ME: Переименовала переменную n
    //int n, value, M;
    int count, value, M;

     //FIX ME: Проверка ввода количества элементов
    //std::cout << "Введите количество элементов в списке: ";
    //std::cin >> count;
    //if (count <= 0)
    //{
    //    std::cout << "Количество элементов должно быть положительным!" << std::endl;
    //    return 1;
    //}
    // Ввод количества элементов
    count = getValidIntInput("Введите количество элементов в списке: ", true);

 
    // 
    ////FIX ME: Добавила понятный ввод элементов
    //std::cout << "Введите элементы списка: ";
    //for (int i = 0; i < n; ++i)
    //{
    //    std::cin >> value;               // Вводим значение элемента
    //    list.Add(value);                 // Добавляем элемент в список
    //}
    std::cout << "Введите элементы списка:" << std::endl;
    for (int i = 0; i < count; ++i)
    {
        std::string prompt = " Элемент " + std::to_string(i + 1) + ": ";
        value = getValidIntInput(prompt, false);
        list.Add(value);
    }
    //FIX ME: Проверка ввода M
    //std::cout << "Введите значение M для вставки: ";

    M = getValidIntInput("Введите значение M для вставки: ", false);
    //FIX ME: Добавила вывод исходного списка
    std::cout << "Исходный список: ";
    list.Print();

    // Вставка значения M перед каждым вторым элементом
    InsertBeforeEverySecond(list, M);


    std::cout << "Список после вставки: ";
    list.Print();

    // Получение указателя на последний элемент списка
    Node* lastNode = list.GetLastNode();

    // Вывод результата
    if (lastNode != nullptr) 
    {
        std::cout << "Указатель на последний элемент списка: " << lastNode << std::endl;
        std::cout << "Значение последнего элемента: " << lastNode->data << std::endl;
    }
    else
    {
        std::cout << "Список пуст." << std::endl;

    }
    list.ClearList();

    // Проверка, что список пуст
    std::cout << "Проверка списка после очистки: ";
    list.Print();


    return 0;
}