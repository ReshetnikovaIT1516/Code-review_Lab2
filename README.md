# Решетникова Анастасия ИТ-15 Вариант 18
## Задача 1  
* Код был разделен на модули: Header.h, Source.cpp, и main.cpp
* Был использован using namespace std, заменено на std::
* Добавлены недостающие библиотеки #include <sstream> и #include <string>.
* Все открывающие фигурные скобки { перенесены на новые строки для единообразия и лучшей читаемости.
* В случае ошибки открытия файла программа теперь возвращает код ошибки return 1;.
* Вместо прямого считывания input >> N >> Z; теперь используется функция SafeReadInt для построчного и более безопасного чтения с проверкой на ошибки.
* Добавлены проверки, что N является положительным числом (N <= 0), а Z — неотрицательным (Z < 0).
* После считывания N и Z добавлен getline(input, skipLine);, чтобы корректно перейти на следующую строку файла.
* Перед считыванием весов (строка 2) и налогов (строка 3) вызывается функция HasEnoughNumbersInLine, которая проверяет, содержит ли строка ровно N чисел и не содержит ли букв.
* После считывания массивов weights и taxes добавлены циклы, проверяющие, что все значения являются неотрицательными. При обнаружении отрицательного значения программа завершается с ошибкой.
* После считывания всех необходимых данных выполняется проверка на наличие в файле неиспользованных символов, и выводится предупреждение.
* В конце функции main добавлен return 0;.
* Переименовала файл с "t.txt" на "T.txt" (с заглавной буквы).
* Переименование переменных в KnapSack
  

## Задача 2
* Код был разделен на модули: Header.h, Source.cpp, и main.cpp
* Был использован using namespace std, заменено на std
* Все открывающие фигурные скобки { перенесены на новые строки для единообразия и лучшей читаемости.
* Добавлена строка setlocale(LC_ALL, "rus"); для корректного вывода на русском языке.
* Имена файлов изменены с "a.txt" и "b.txt" на "A.txt" и "B.txt" (с заглавной буквы).
* Добавлен вызов CheckFileOpen(input, output) для проверки успешности открытия файлов.
* Вместо прямого считывания input >> N теперь используется функция ReadAndCheckN.
* Добавлен вызов CheckBoardDimensions(input, N), который проверяет, что в файле достаточно строк для доски размером N×N.
* Переименование переменной: docka на board.
* Вместо прямого чтения теперь используется функция ReadAndCheckBoard.
* Добавлен вызов CheckExtraCharacters(input) для обнаружения лишних данных после основной информации.
* В конце добавлен std::cout << "Ответ записан в файле b.txt" << std::endl; для информирования пользователя.


## Задача 3
* Код был разделен на модули: Header.h, Source.cpp, и main.cpp
* Был использован using namespace std, заменено на std::
* Все открывающие фигурные скобки { перенесены на новые строки для единообразия и лучшей читаемости.
* Убрана #include <fstream>, так как ввод осуществляется с консоли, а не из файла.
* Переименование функций:
Count на CountNumbersWithFourConsecutiveZeros 
HomeDyn16 на ProcessKRadixNumbersTask 
* Исправление логической ошибки:
В исходном коде было: dp[1][1] = 1; (первая цифра может быть нулем).
Исправлено на: countingTable[1][1] = 0; (первая цифра не может быть нулем, так как число не может начинаться с нуля).
* Добавление проверок ввода для K.
* Добавление проверок ввода для N.
* Исправление формулы общего количества чисел:
В исходном коде: double all = pow(K, N); (некорректно, включает числа, начинающиеся с нуля).
Исправлено на: double totalPossibleNumbers = (K - 1) * pow(K, N - 1); (первая цифра от 1 до K-1, остальные любые).

## Задача 4
* Код был разделен на модули: Header.h, Source.cpp, и main.cpp.
* Был использован using namespace std, заменено на std::
* Все открывающие фигурные скобки { перенесены на новые строки для единообразия и лучшей читаемости.
* Добавлена #include <cctype> для использования функции isdigit().
* Добавлена строка с указанием допустимых символов: "Допустимые символы: цифры, +, -, *, (, )".
* Переименование переменных на более понятные.
* Добавление проверки корректности ввода.
* Переименование всех функций (с заглавной буквы).
* Создание рекурсивных функций поиска операторов.
* Замена циклов на рекурсию (согласно условию).

## Задача 5
* Код был разделен на модули: Header.h, Source.cpp, и main.cpp.
* Был использован using namespace std, заменено на std::
* Все открывающие фигурные скобки { перенесены на новые строки для единообразия и лучшей читаемости.
