# Решетникова Анастасия ИТ-15 Вариант 18
## Задача 1
* Код был разделен на модули: Header.h, Source.cpp, и lab2_task1.cpp
* Был использован using namespace std, заменено на std::
* Добавлены недостающие библиотеки #include <string>, <sstream>, <cctype>
* Все открывающие фигурные скобки { перенесены на новые строки для единообразия и лучшей читаемости.
* Вместо прямого считывания cin >> N; теперь используется функция GetValidNumber для безопасного ввода с проверкой на ошибки (пустой ввод, буквы, знак минус).
* Добавлен деструктор ~Stack() для автоматической очистки памяти и предотвращения утечек.
* Добавлен метод NumberDelete() для удаления элемента из стека (стандартный метод удаления).
* Улучшен метод Show(): теперь проверяет, пуст ли стек, и выводит соответствующее сообщение.
* Метод getTopValue() заменен на GetTopValue(int& value), который возвращает bool и значение через параметр для безопасной работы с пустым стеком.
* Метод isEmpty() переименован в IsEmpty() для единообразия (все методы с заглавной буквы).
* В main добавлена установка локали и кодировки консоли для поддержки русского языка.
* Ввод чисел сделан с приглашениями "Введите число 1: ", "Введите число 2: " и т.д. для удобства пользователя.
* Добавлена демонстрация работы метода удаления элемента в конце программы.
* Добавлена проверка на пустой ввод в функции GetValidNumber.


## Задача 2
* Код был разделен на модули: Header.h, Source.cpp, и lab2_task2.cpp
* Был использован using namespace std, заменено на std::
* Добавлены недостающие библиотеки #include <windows.h>, <string>, <cctype>, <limits>
* Все открывающие фигурные скобки { перенесены на новые строки для единообразия и лучшей читаемости.
* Вместо жестко заданных 10 элементов теперь используется ввод с клавиатуры с проверкой на минимальное количество (N >= 5).
* Добавлена функция GetValidNumber для безопасного ввода с проверкой на ошибки (пустой ввод, буквы, знак минус для отрицательных чисел).
* Добавлена функция IsNumber для проверки строки на соответствие целому числу.
* Добавлена функция PrintQueue для вывода всех элементов очереди (стандартный метод вывода).
* Переименованы поля структуры PQueue: front → Head, last → Tail для единообразия и понятности.
* В main добавлена установка локали и кодировки консоли для поддержки русского языка.
* Ввод чисел сделан с приглашениями "Элемент 1: ", "Элемент 2: " и т.д. для удобства пользователя.
* Добавлен вывод оставшейся (результирующей) очереди после извлечения пяти элементов.
* Добавлена проверка на отрицательные числа при вводе количества элементов (только положительные).
* Добавлена обработка исключений при преобразовании строки в число (std::stoi может выбросить исключение).


## Задача 3
* Код был разделен на модули: Header.h, Source.cpp, и lab2_task3.cpp
* Был использован using namespace std, заменено на std::
* Добавлены недостающие библиотеки #include <windows.h>, <sstream>, <string>, <cctype>, <limits>
* Все открывающие фигурные скобки { перенесены на новые строки для единообразия и лучшей читаемости.
* Вместо прямого считывания cin >> n; теперь используется функция getValidIntInput для безопасного ввода с проверкой на ошибки (пустой ввод, запятые/точки, буквы).
* Добавлены проверки, что количество элементов положительное (positiveOnly = true) при вводе count.
* Создан класс LinkedList с инкапсуляцией данных (head в private) и стандартным набором методов (Add, InsertBeforeEverySecond, Print, GetLastNode, ClearList).
* Класс Node вынесен отдельно, конструктор Node реализован в Source.cpp.
* Добавлен деструктор ~LinkedList() для автоматической очистки памяти, чтобы избежать утечек.
* Функции переименованы с заглавной буквы: add → Add, print → Print, getLastNode → GetLastNode, clearList → ClearList.
* В main добавлена установка локали и кодировки консоли для поддержки русского языка.
* Ввод чисел сделан с приглашениями "Элемент 1: ", "Элемент 2: " и т.д. для удобства пользователя.
* Добавлен вывод исходного списка до вставки.
* Добавлена проверка на пустой список при получении последнего элемента.
* ИСПРАВЛЕН АЛГОРИТМ InsertBeforeEverySecond: старая версия вставляла элементы неправильно (использовала count % 2 == 1).
  Новая версия корректно вставляет значение M перед каждым вторым элементом (четные позиции 2, 4, 6...)
  с использованием указателя prev для правильной связки узлов.


## Задача 4
* Код был разделен на модули: Header.h, Source.cpp, и lab2_task4.cpp
* Был использован using namespace std, заменено на std::
* Добавлены недостающие библиотеки #include <windows.h>, <sstream>, <string>, <cctype>, <limits>
* Все открывающие фигурные скобки { перенесены на новые строки для единообразия и лучшей читаемости.
* Вместо прямого считывания cin >> n; теперь используется функция GetValidIntInput для безопасного ввода с проверкой на ошибки (пустой ввод, запятые/точки, буквы).
* Добавлены проверки, что количество чисел положительное (positiveOnly = true) при вводе.
* Создан класс DoubleList с инкапсуляцией данных (head в private) и стандартным набором методов (PushBack, PrintList, DeleteNode, RemoveSides).
* Структура Node вынесена внутрь класса DoubleList, конструктор Node реализован отдельно в Source.cpp.
* Добавлен геттер GetHead() для доступа к head при необходимости.
* В деструкторе переименована переменная curr на current для понятности.
* Функции переименованы с заглавной буквы: push_back → PushBack, printList → PrintList, removeSides → RemoveSides, deleteNode → DeleteNode.
* В main добавлена установка локали и кодировки консоли для поддержки русского языка.
* Ввод чисел сделан с приглашениями "Число 1: ", "Число 2: " и т.д. для удобства пользователя.
* Добавлена проверка на пустой список при выводе (выводит "Список пуст").
* Добавлена проверка на существование tail перед выводом последнего элемента.
* ИСПРАВЛЕН АЛГОРИТМ RemoveSides: старая версия удаляла элементы во время обхода, что могло приводить к ошибкам. Новая версия сначала подсчитывает размер списка, затем в два прохода: первый - отмечает элементы для удаления в массиве, второй - удаляет их. Это гарантирует корректность работы с циклическим списком.


## Задача 5
* Код был разделен на модули: Header.h, Source.cpp, и lab2_task5.cpp
* Был использован using namespace std, заменено на std::
* Добавлены недостающие библиотеки #include <windows.h>, <sstream>, <string>, <vector>, <algorithm>, <limits>
* Все открывающие фигурные скобки { перенесены на новые строки для единообразия и лучшей читаемости.
* Вместо прямого считывания cin >> choice; теперь используется функция SafeInputInt для безопасного ввода с проверкой на ошибки.
* Добавлены проверки, что количество чисел нечетное и >= 5 во всех функциях ввода.
* Добавлен класс DequeContainer с дружественными функциями для инкапсуляции дека.
* Добавлен конструктор с итераторами для заполнения контейнера (DequeContainer(Iterator begin, Iterator end)).
* Добавлены функции PrintDequeForward (с обычными итераторами) и PrintDequeReverse (с обратными итераторами rbegin/rend) для вывода.
* В FuncFile добавлена детальная проверка каждого токена на число с помощью IsValidNumber и указанием номера строки при ошибке.
* Переименование переменных: N на sizeMas в FuncInsert, it на iter, rit на reverseIter.
* Переименовала файл с "a.txt" на "A.txt" (с заглавной буквы).
* Добавлена установка локали и кодировки консоли для поддержки русского языка.
